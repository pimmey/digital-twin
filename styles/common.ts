import { StyleSheet } from 'react-native'

const commonStyles = StyleSheet.create({
  textActive: {
    color: 'black'
  },
  textDisabled: {
    color: 'lightgray'
  }
})

export default commonStyles
